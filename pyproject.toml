[project]
name = "autoexperiment"
version = "0.1.0"
description = "Hypothesis-driven autoresearch loop with causal model output"
requires-python = ">=3.8"
dependencies = [
    "torch",
    "numpy",
]

[tool.uv]
dev-dependencies = []
